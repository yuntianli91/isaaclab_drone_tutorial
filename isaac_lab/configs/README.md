# Configurations

本目录用于保存后续环境、Agent 和训练配置：

```text
configs/
├── env/
├── agent/
└── train/
```

Tutorial 0 只有场景创建参数，并通过独立入口的命令行参数传入，因此当前不创建没有消费者的
YAML。进入 Environment 和 Agent Tutorial 后，再添加对应配置文件及其加载入口。
